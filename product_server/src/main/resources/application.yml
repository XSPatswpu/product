# eureka-client配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: product
# 数据源
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: xsp520
    url: jdbc:mysql://127.0.0.1:3306/cherry-cell?characterEncoding=utf-8&useSSL=false
# jpa设置
  jpa:
    show-sql: true
server:
  port: 8888